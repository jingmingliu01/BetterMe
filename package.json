{
  "name": "betterme",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm --workspace apps/extension run dev",
    "build": "npm --workspace apps/extension run build",
    "generate:ai-check-contract": "npm --workspace apps/extension run generate:ai-check-contract",
    "validate:ai-check-contract": "npm --workspace apps/extension run validate:ai-check-contract",
    "check:ai-check-contract": "npm --workspace apps/extension run check:ai-check-contract",
    "typecheck": "npm --workspace apps/extension run typecheck"
  },
  "workspaces": [
    "apps/extension"
  ]
}
