{
  "name": "betterme",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm --workspace apps/extension run dev",
    "build": "npm --workspace apps/extension run build",
    "typecheck": "npm --workspace apps/extension run typecheck"
  },
  "workspaces": [
    "apps/extension"
  ]
}
